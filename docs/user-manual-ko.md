# ZYNK 사용자 매뉴얼

## 목차
1. [API 연결 설정](#1-api-연결-설정)
2. [에이전트팀 인스턴스 할당](#2-에이전트팀-인스턴스-할당)

---

## 1. API 연결 설정

### 개요
소셜미디어 채널(X, Instagram, YouTube 등)을 사용하기 위해서는 먼저 API 인증 정보를 등록해야 합니다.

### 1.1 API 연결 추가하기

#### 단계 1: Settings 메뉴 접속
1. 좌측 사이드바에서 **Settings** 클릭
2. **Connections** 탭 선택
3. 우측 상단의 **+ Add Channel** 버튼 클릭

#### 단계 2: 채널 정보 입력

**필수 입력 항목:**

1. **Channel*** (채널 선택)
   - 사용할 소셜미디어 플랫폼 선택
   - 예: X (Twitter), Instagram, YouTube 등

2. **Project*** (프로젝트 선택)
   - 이 API를 사용할 프로젝트 선택
   - 드롭다운에서 본인이 생성한 프로젝트 목록이 표시됩니다
   - 프로젝트가 없다면 먼저 프로젝트를 생성해야 합니다

3. **Agent Team** (선택사항)
   - 이 API를 즉시 연결할 에이전트팀 선택
   - 나중에 연결할 수도 있으므로 선택사항입니다
   - 해당 프로젝트에서 선택한 채널을 사용하는 에이전트팀만 표시됩니다

4. **Detailed Name*** (상세 이름)
   - 이 API 연결을 구분하기 위한 이름
   - 예: "VisionChain 공식 X 계정", "마케팅용 Instagram"

5. **API 인증 정보**
   - 선택한 채널에 따라 필요한 필드가 자동으로 표시됩니다
   - 예: API Key, API Secret, Access Token 등

#### 단계 3: 연결 테스트 및 저장

1. **Test Connection** 버튼 클릭 (권장)
   - API 인증 정보가 올바른지 확인
   - 성공 시: ✅ "Connection successful" 메시지
   - 실패 시: ❌ 오류 메시지 확인 후 정보 수정

2. **Save Connection** 버튼 클릭
   - API 연결 정보 저장
   - Agent Team을 선택했다면 자동으로 연결됩니다

### 1.2 API 연결 상태 확인

Connections 탭의 테이블에서 다음 정보를 확인할 수 있습니다:

| 컬럼 | 설명 |
|------|------|
| **Channel** | 소셜미디어 플랫폼 (X, Instagram 등) |
| **Account** | 입력한 Detailed Name |
| **Agent Team** | 연결된 에이전트팀 상태<br>● (초록불) = 연결됨<br>○ (회색) = 연결 안됨 |
| **Status** | API 연결 상태<br>✓ Connected = 정상<br>⚠ Not tested = 미검증 |
| **Last Tested** | 마지막 테스트 시간 |
| **Actions** | Edit (수정) / Delete (삭제) |

### 1.3 API 연결 수정하기

1. 수정할 연결의 **Edit** 버튼 클릭
2. 정보 수정
3. **Save Connection** 클릭

### 1.4 API 연결 삭제하기

1. 삭제할 연결의 **Delete** 버튼 클릭
2. 확인 메시지에서 **확인** 클릭

⚠️ **주의**: 삭제하면 해당 API를 사용하는 에이전트팀이 작동하지 않을 수 있습니다.

---

## 2. 에이전트팀 인스턴스 할당

### 개요
에이전트팀 인스턴스는 실제로 작업을 수행하는 AI 에이전트들의 그룹입니다. 프로젝트에 에이전트팀을 할당하여 자동화 작업을 시작할 수 있습니다.

### 2.1 에이전트팀 인스턴스 생성하기

#### 단계 1: Deploy Wizard 시작

1. **Command Center** 메뉴 접속
2. 프로젝트 카드에서 **+ Deploy New Agent** 버튼 클릭
3. Deploy Wizard가 시작됩니다 (3단계)

#### 단계 2: Step 1 - 기본 정보 및 인증

**필수 입력 항목:**

1. **Select Channel***
   - 에이전트팀이 사용할 소셜미디어 채널 선택
   - 예: X (Twitter), Instagram 등

2. **API Credential**
   - Settings에서 등록한 API 연결 선택
   - 드롭다운에 현재 프로젝트의 해당 채널 API만 표시됩니다
   
   **상황별 안내:**
   
   - ✅ **API가 있는 경우**: 
     - 드롭다운에서 선택
     - ✓ (초록 체크) = 연결 테스트 완료
     - ⚠ (경고) = 연결 미검증
   
   - ❌ **API가 없는 경우**:
     - "No credential available" 메시지 표시
     - **Go to Settings** 버튼 클릭하여 API 먼저 등록
     - 또는 **Continue Anyway** 클릭하여 나중에 연결

3. **Team Name***
   - 에이전트팀 이름 입력
   - 예: "[X] 마케팅 팀", "[Instagram] 콘텐츠 제작팀"

4. **Description** (선택사항)
   - 에이전트팀의 역할 설명

**Next Step** 버튼 클릭

#### 단계 3: Step 2 - 템플릿 선택

1. 사용할 에이전트팀 템플릿 선택
   - 각 템플릿은 미리 구성된 서브에이전트들을 포함합니다
   - 템플릿 카드를 클릭하여 선택

2. **Next Step** 버튼 클릭

#### 단계 4: Step 3 - 최종 확인

1. 설정 내용 확인:
   - 채널 정보
   - API 연결 상태
   - 템플릿 정보
   - 서브에이전트 목록

2. **Create Agent Team** 버튼 클릭

3. 생성 완료!
   - 에이전트팀 카드가 Command Center에 표시됩니다
   - 상태: ACTIVE (활성)

### 2.2 에이전트팀 상태 확인

Command Center의 에이전트팀 카드에서 다음 정보를 확인할 수 있습니다:

```
┌─────────────────────────────────────┐
│ [X] VisionChain Marketing Team  ⚠️ 1│
│                                     │
│ Autonomous Agent Team               │
│                                     │
│ ⚡ ACTIVE DIRECTIVE:                │
│ System initialized. Waiting for     │
│ task assignment.                    │
│                                     │
│ Daily Actions: 0/15                 │
│ Total Runs: 0                       │
│                                     │
│ [View History] [⚙️]                 │
└─────────────────────────────────────┘
```

**상태 표시:**
- ⚠️ 숫자 = 주의가 필요한 항목 수 (예: API 미연결)
- ACTIVE = 활성 상태
- Daily Actions = 오늘 수행한 작업 수

### 2.3 에이전트팀 상세 정보 보기

1. 에이전트팀 카드에서 **View History** 버튼 클릭
2. 하단에 상세 패널이 표시됩니다

**좌측: Assigned Sub-Agents**

```
Channel Connections
├─ X (Twitter)  ● Ready
   [Check Connection]

서브에이전트 목록:
├─ 콘텐츠기획설계 Agent  100% SR
│  planner
│  Last active: No activity yet
│  🤖 Not configured
│
├─ 텍스트생성 에이전트  100% SR
│  creator_text
│  Last active: No activity yet
│  🤖 Not configured
```

**Channel Connections 섹션:**
- ● (초록불) = API 연결 완료, 정상 작동
- ⚠️ Missing Key = API 미연결
- **Check Connection** 버튼:
  - 클릭 시 API 연결 상태 실시간 확인
  - ✓ Connected = 정상
  - ✗ Failed = 오류

**우측: Recent Runs**
- 최근 실행 기록 표시
- 각 실행의 성공/실패 여부 확인

### 2.4 API 연결 상태 확인 및 재연결

#### 방법 1: View History에서 확인

1. 에이전트팀 카드 → **View History** 클릭
2. Channel Connections 섹션 확인
3. **Check Connection** 버튼 클릭
   - "Checking..." 표시
   - 2초 후 결과 표시:
     - ✓ Connected (초록색) = 성공
     - ✗ Failed (빨간색) = 실패

#### 방법 2: Settings에서 재연결

1. **Settings** → **Connections** 이동
2. Agent Team 컬럼에서 연결 상태 확인
   - ● (초록불) = 연결됨
   - ○ (회색) = 미연결
3. 미연결 상태라면:
   - **Edit** 버튼 클릭
   - **Agent Team** 드롭다운에서 연결할 팀 선택
   - **Save Connection** 클릭

### 2.5 에이전트팀 설정 변경

1. 에이전트팀 카드에서 **⚙️** (설정) 버튼 클릭
2. 설정 모달에서 수정:
   - 팀 이름
   - 설명
   - 기타 설정
3. **Save** 클릭

---

## 자주 묻는 질문 (FAQ)

### Q1: API를 먼저 등록해야 하나요, 에이전트팀을 먼저 만들어야 하나요?

**A**: 두 가지 방법 모두 가능합니다.

**권장 순서 (초보자):**
1. Settings → Connections에서 API 먼저 등록
2. Deploy Wizard에서 에이전트팀 생성 시 API 자동 선택

**대안 (고급 사용자):**
1. Deploy Wizard에서 에이전트팀 먼저 생성 (API 없이)
2. Settings → Connections에서 API 등록 시 에이전트팀 선택하여 연결

### Q2: 하나의 API를 여러 에이전트팀에서 사용할 수 있나요?

**A**: 네, 가능합니다. 같은 X 계정을 여러 에이전트팀(마케팅, 고객지원 등)에서 공유할 수 있습니다.

Settings → Connections 테이블에서 Agent Team 컬럼에 여러 팀이 표시됩니다:
```
● Marketing Team
● Support Team
```

### Q3: 에이전트팀에 API가 연결되지 않으면 어떻게 되나요?

**A**: 에이전트팀은 생성되지만 실제 작업을 수행할 수 없습니다.

- 에이전트팀 카드에 ⚠️ 경고 표시
- Channel Connections에 "⚠️ Missing Key" 표시
- Settings에서 API 연결 후 자동으로 작동 시작

### Q4: API 연결 테스트는 필수인가요?

**A**: 필수는 아니지만 **강력히 권장**합니다.

- Test Connection을 하지 않으면 Status가 "⚠ Not tested"로 표시
- 실제 사용 시 오류가 발생할 수 있음
- 테스트 성공 시 "✓ Connected"로 변경되어 안심하고 사용 가능

### Q5: 프로젝트를 잘못 선택했어요. 어떻게 수정하나요?

**A**: Settings → Connections에서 수정할 수 있습니다.

1. 해당 API 연결의 **Edit** 버튼 클릭
2. **Project** 드롭다운에서 올바른 프로젝트 선택
3. **Agent Team** 드롭다운이 자동으로 업데이트됨
4. **Save Connection** 클릭

### Q6: "Already has credential" 경고는 무엇인가요?

**A**: 해당 에이전트팀이 이미 다른 API를 사용 중이라는 의미입니다.

- 하나의 에이전트팀은 하나의 채널당 하나의 API만 사용 가능
- 교체하려면 기존 API를 선택하여 덮어쓰기
- 새로운 API를 추가하려면 다른 에이전트팀 선택

### Q7: Check Connection 버튼은 언제 사용하나요?

**A**: 다음 상황에서 사용합니다:

1. **정기 점검**: 주기적으로 API 연결 상태 확인
2. **오류 발생 시**: 에이전트팀이 작동하지 않을 때
3. **API 변경 후**: Settings에서 API 정보를 수정한 후
4. **재시작**: API 연결을 새로고침하고 싶을 때

---

## 문제 해결 (Troubleshooting)

### 문제 1: "No credential available" 메시지가 표시됩니다

**원인**: 선택한 채널에 대한 API가 등록되지 않았습니다.

**해결방법**:
1. **Go to Settings** 버튼 클릭
2. Settings → Connections → Add Channel
3. 해당 채널의 API 등록
4. Deploy Wizard로 돌아와서 계속 진행

### 문제 2: Check Connection 시 "✗ Failed" 표시됩니다

**원인**: API 인증 정보가 잘못되었거나 만료되었습니다.

**해결방법**:
1. Settings → Connections 이동
2. 해당 API의 **Edit** 버튼 클릭
3. API Key, Secret 등 재확인 및 수정
4. **Test Connection** 클릭하여 확인
5. **Save Connection** 클릭

### 문제 3: Agent Team 드롭다운이 비어있습니다

**원인**: 선택한 프로젝트에 해당 채널을 사용하는 에이전트팀이 없습니다.

**해결방법**:
- **옵션 1**: "-- None (add later)" 선택하여 일단 저장
- **옵션 2**: Deploy Wizard에서 에이전트팀 먼저 생성 후 다시 시도

### 문제 4: 에이전트팀이 작동하지 않습니다

**확인 사항**:
1. ✅ API가 연결되어 있는지 확인 (● 초록불)
2. ✅ Check Connection 시 "✓ Connected" 표시되는지 확인
3. ✅ 에이전트팀 상태가 ACTIVE인지 확인
4. ✅ 서브에이전트가 configured 상태인지 확인

**해결방법**:
- View History → Check Connection 클릭
- 실패 시 Settings에서 API 재설정

---

## 추가 도움말

더 자세한 정보가 필요하시면:
- 📧 고객지원: support@zynk.ai
- 📚 온라인 문서: docs.zynk.ai
- 💬 커뮤니티: community.zynk.ai

---

**마지막 업데이트**: 2025-12-05  
**버전**: 1.0
