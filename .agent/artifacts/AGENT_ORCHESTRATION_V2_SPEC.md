# 🚀 Agent Orchestration V2 - 상세 기획서

> **Version**: 2.0.0  
> **Created**: 2024-12-07  
> **Author**: AI Agent System  

---

## 📌 현재 문제점

### 1. 순차 실행 비효율
```
현재: Planner → Text Creator → Image Creator → Reviewer (순차)
개선: Planner → [Text Creator + Image Creator] (병렬) → Reviewer
```

### 2. Generated Content에 모든 출력이 혼재
```
현재:
┌─────────────────────────────────────┐
│         Generated Content           │
├─────────────────────────────────────┤
│ 📋 Content Plan (Internal)          │ ← 이건 작업 로그
│ 📝 Generated Text (Publishable)     │ ← 이건 게시 가능 콘텐츠
│ 🖼️ Generated Image (Publishable)    │ ← 이건 게시 가능 콘텐츠
│ ✅ Compliance Review (Internal)     │ ← 이건 작업 로그
│ 🔍 Market Research (Internal)       │ ← 이건 작업 로그
└─────────────────────────────────────┘
```

---

## 🎯 개선 목표

### 1. 병렬 실행 구현
- Planner가 작업 계획을 생성한 후, 텍스트/이미지 생성을 **동시에** 실행
- 실행 시간 단축 (예: 10초 → 6초)

### 2. UI 분리
- **Generated Content**: 게시 가능한 콘텐츠만 표시 (Text, Image, Video)
- **Agent Work Log**: 내부 작업 로그 표시 (Plan, Research, Review)

---

## 🏗️ Phase 1: UI 구조 개선

### 1.1 새로운 탭 구조

```
┌─────────────────────────────────────────────────────────┐
│ Generated Content                                       │
├─────────────────────────────────────────────────────────┤
│  [📄 Content]  [📋 Work Log]                            │  ← 탭 버튼
├─────────────────────────────────────────────────────────┤
│                                                         │
│  (선택된 탭의 내용이 여기에 표시)                          │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 1.2 Content 탭 (게시 가능 콘텐츠)
| 항목 | 설명 |
|-----|------|
| 📝 텍스트 | X/Twitter, Instagram 캡션 등 |
| 🖼️ 이미지 | DALL-E 생성 이미지 |
| 🎬 비디오 | 비디오 스크립트/콘텐츠 |
| 플랫폼 미리보기 | X, Instagram, Facebook 프레임 |
| 액션 버튼 | Copy, Schedule, Approve |

### 1.3 Work Log 탭 (내부 작업 로그)
| 항목 | 설명 |
|-----|------|
| 📋 Content Plan | Planner 에이전트 출력 |
| 🔍 Market Research | Research 에이전트 출력 |
| ✅ Compliance Review | Compliance 에이전트 출력 |
| 👔 Manager Summary | Manager 에이전트 출력 |
| 📊 Analytics Report | Analytics 에이전트 출력 |
| 타임라인 뷰 | 각 에이전트 실행 순서와 시간 |

---

## 🏗️ Phase 2: 병렬 실행 로직

### 2.1 실행 스테이지 정의

```
Stage 1: Planning (순차)
├── Planner Agent
└── Research Agent (optional)

Stage 2: Creation (병렬)
├── Text Creator Agent ──┐
├── Image Creator Agent ──├── Promise.all()
└── Video Creator Agent ──┘

Stage 3: Review (순차)
├── Compliance Agent
└── Manager Agent

Stage 4: Finalize (순차)
└── Final Content Assembly
```

### 2.2 Planner 출력 구조 개선

```json
{
  "plan_id": "plan_xxx",
  "overall_goal": "Vision Chain 홍보 콘텐츠",
  "target_audience": "Web3 개발자, DeFi 투자자",
  
  "text_instructions": {
    "platform": "x",
    "tone": "professional, innovative",
    "key_messages": ["cross-chain payment", "zero slippage"],
    "hashtags": ["#VisionChain", "#DeFi", "#Web3"],
    "max_length": 280
  },
  
  "image_instructions": {
    "style": "modern, tech, gradient",
    "elements": ["blockchain network", "payment flow"],
    "colors": ["#16e0bd", "#8b5cf6"],
    "aspect_ratio": "1:1"
  },
  
  "video_instructions": null
}
```

### 2.3 병렬 실행 코드

```javascript
async executeParallelCreation(plannerOutput, subAgents, context) {
    const plan = JSON.parse(plannerOutput);
    
    // 병렬 실행할 에이전트 찾기
    const textAgent = subAgents.find(a => a.role_type === 'creator_text');
    const imageAgent = subAgents.find(a => a.role_type === 'creator_image');
    
    // 병렬 실행 준비
    const parallelTasks = [];
    
    if (textAgent && plan.text_instructions) {
        parallelTasks.push(
            this._executeSubAgent(textAgent, {
                ...context,
                specificInstructions: plan.text_instructions
            })
        );
    }
    
    if (imageAgent && plan.image_instructions) {
        parallelTasks.push(
            this._executeSubAgent(imageAgent, {
                ...context,
                specificInstructions: plan.image_instructions
            })
        );
    }
    
    // 동시 실행
    const results = await Promise.all(parallelTasks);
    
    return results;
}
```

---

## 🏗️ Phase 3: 데이터 구조 개선

### 3.1 generatedContents 컬렉션 분류

```javascript
// 게시 가능 콘텐츠
{
    content_category: 'publishable',  // 새 필드
    content_type: 'text' | 'image' | 'video',
    platform: 'x' | 'instagram' | 'facebook',
    status: 'pending' | 'approved' | 'scheduled' | 'published'
}

// 작업 로그
{
    content_category: 'work_log',     // 새 필드
    content_type: 'meta',
    log_type: 'plan' | 'research' | 'review' | 'analytics',
    is_internal: true
}
```

### 3.2 agentRuns 실행 로그 개선

```javascript
{
    run_id: 'run_xxx',
    execution_mode: 'parallel',  // 새 필드: 'sequential' | 'parallel'
    stages: [
        {
            stage: 'planning',
            agents: ['planner'],
            status: 'completed',
            duration_ms: 2500
        },
        {
            stage: 'creation',
            agents: ['creator_text', 'creator_image'],
            parallel: true,       // 병렬 실행 여부
            status: 'completed',
            duration_ms: 3000     // 가장 오래 걸린 에이전트 기준
        },
        {
            stage: 'review',
            agents: ['compliance', 'manager'],
            status: 'completed',
            duration_ms: 1500
        }
    ],
    total_duration_ms: 7000
}
```

---

## 📋 개발 작업 계획

### Phase 1: UI 구조 개선 (1시간)
| 순서 | 작업 | 담당 파일 |
|-----|------|---------|
| 1.1 | Content/Work Log 탭 UI 추가 | `project-detail.html` |
| 1.2 | 탭 전환 CSS 스타일 | `admin-detail.css` |
| 1.3 | 탭 전환 JavaScript | `mission-control-view-history.js` |
| 1.4 | 필터링 로직 (publishable vs meta) | `mission-control-view-history.js` |

### Phase 2: 병렬 실행 로직 (2시간)
| 순서 | 작업 | 담당 파일 |
|-----|------|---------|
| 2.1 | 스테이지 정의 상수 | `agent-execution-service.js` |
| 2.2 | Planner 프롬프트 개선 | `agent-execution-service.js` |
| 2.3 | 병렬 실행 함수 구현 | `agent-execution-service.js` |
| 2.4 | executeRun 리팩토링 | `agent-execution-service.js` |

### Phase 3: 데이터 구조 개선 (30분)
| 순서 | 작업 | 담당 파일 |
|-----|------|---------|
| 3.1 | content_category 필드 추가 | `agent-execution-service.js` |
| 3.2 | 기존 데이터 마이그레이션 스크립트 | `scripts/migrate-content-category.js` |

---

## ✅ 구현 체크리스트

### Phase 1: UI
- [ ] Content/Work Log 탭 버튼 추가
- [ ] 탭 전환 로직 구현
- [ ] Content 탭: publishable 콘텐츠만 표시
- [ ] Work Log 탭: meta 콘텐츠만 표시
- [ ] Work Log 탭: 타임라인 스타일 UI

### Phase 2: 병렬 실행
- [ ] Planner 프롬프트에 JSON 출력 구조 추가
- [ ] 병렬 실행 함수 `executeParallelCreation` 구현
- [ ] 스테이지 기반 실행 로직 구현
- [ ] 실행 로그에 스테이지 정보 저장

### Phase 3: 데이터
- [ ] `content_category` 필드 추가
- [ ] 저장 로직에 category 분류 추가
- [ ] 마이그레이션 스크립트 실행

---

## 📐 UI 와이어프레임

### Content 탭 (활성화)
```
┌─────────────────────────────────────────────────────────┐
│ Generated Content                                       │
├─────────────────────────────────────────────────────────┤
│  [📄 Content]▼  [📋 Work Log]                           │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │         X (Twitter) Preview                      │   │
│  │  ┌──┐  Vision Chain ✓ @visionchain              │   │
│  │  │🟢│                                            │   │
│  │  └──┘  🚀 Introducing Vision Chain X!          │   │
│  │       The revolutionary cross-chain...          │   │
│  │                                                  │   │
│  │  💬 0  🔄 0  ❤️ 0  📊 0  🔖  📤                │   │
│  ├──────────────────────────────────────────────────┤   │
│  │  [📋 Copy]   [📅 Schedule]   [✓ Approve]        │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │         🖼️ Generated Image                       │   │
│  │                                                  │   │
│  │        [Generated Image Preview]                 │   │
│  │                                                  │   │
│  ├──────────────────────────────────────────────────┤   │
│  │  [📋 Copy]   [📅 Schedule]   [✓ Approve]        │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### Work Log 탭 (활성화)
```
┌─────────────────────────────────────────────────────────┐
│ Generated Content                                       │
├─────────────────────────────────────────────────────────┤
│  [📄 Content]  [📋 Work Log]▼                           │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Run: 2024-12-07 16:45 • 7.2s total                    │
│                                                         │
│  ┌ Stage 1: Planning ────────────────── 2.5s ────────┐ │
│  │                                                    │ │
│  │  📋 Content Plan                      ✅ Complete  │ │
│  │  ───────────────────────────────────────────────  │ │
│  │  Goal: Vision Chain 홍보 콘텐츠                   │ │
│  │  Target: Web3 개발자, DeFi 투자자                 │ │
│  │  Tone: Professional, Innovative                   │ │
│  │                                                    │ │
│  └────────────────────────────────────────────────────┘ │
│                                                         │
│  ┌ Stage 2: Creation (Parallel) ─────── 3.0s ────────┐ │
│  │                                                    │ │
│  │  📝 Text Creator                      ✅ 2.8s     │ │
│  │  🖼️ Image Creator                    ✅ 3.0s     │ │
│  │                                                    │ │
│  └────────────────────────────────────────────────────┘ │
│                                                         │
│  ┌ Stage 3: Review ───────────────────── 1.7s ───────┐ │
│  │                                                    │ │
│  │  ✅ Compliance Review                 ✅ Complete │ │
│  │  ───────────────────────────────────────────────  │ │
│  │  Status: Approved                                  │ │
│  │  Notes: Content complies with guidelines          │ │
│  │                                                    │ │
│  └────────────────────────────────────────────────────┘ │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 🔗 관련 문서

- [Generated Content Preview Spec](./GENERATED_CONTENT_PREVIEW_SPEC.md)
- [Studio V1 Design Spec](./STUDIO_V1_DESIGN_SPEC.md)
- [Agent Execution Plan](../AGENT_EXECUTION_PLAN.md)
